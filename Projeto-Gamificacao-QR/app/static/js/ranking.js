const list=document.querySelector('#rankingList'),status=document.querySelector('#status');async function load(){try{const d=await api('/api/ranking');list.innerHTML=d.ranking.length?d.ranking.map(r=>`<div class="ranking-row"><strong>${r.position}º</strong><span>${esc(r.nick)}</span><strong>${r.points} pts</strong></div>`).join(''):'<p>Ainda não há pontuação registrada.</p>';status.textContent='Atualizado automaticamente.'}catch(e){status.textContent=e.message}}load();setInterval(load,15000);
